import React from 'react';
import Header from '../Home/Header/Header';

const Menus = () => {
    return (
        <div>
            <Header />
            <h2>Inside Menu</h2>
        </div>
    );
};

export default Menus;